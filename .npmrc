# NPM Configuration for Manus Agent v1.0

# Registry settings
registry=https://registry.npmjs.org/

# Authentication (use environment variables)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Package settings
access=public
tag=latest

# Build settings
scripts-prepend-node-path=true
